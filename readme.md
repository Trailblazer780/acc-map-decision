# accmapdecision for Capstone Project 2022 with MySQL

## Available Scripts

In the project directory, you can run:
### `docker compose up --build`
### `dotnet watch run`

To build and run the ASP.NET Core 6 web app

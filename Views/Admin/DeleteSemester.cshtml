@model Semester;

<div>
    <span><h1 class="text-center text-info">ACC Map Decision - Delete Semester  <form asp-controller="Admin" asp-action="Logout" ><input class="btn btn-info" type="Submit" value="Logout"></form></h1></span>
    <span id="lblExpire" class="text-warning" style="margin-left:10px; font-size: medium;"></span>

    <div class="container">
        <div stlye="padding-bottom: 10px">
            <h1 class="text-center text-info">Are you sure you want to delete @Model.semester_code?</h1>
        </div>
        <div><strong>Semester Code:</strong> @Model.semester_code</div>
        <hr>
        <div class="centerDelete">
            <form asp-controller="Admin" asp-action="DeleteSemesterSubmit">
                <input type="hidden" name="id" asp-for="@Model.semester_id">
                <div class="form-row" style="padding-left: 5px;">
                    <div>
                        <span><button class="btn btn-danger" type="submit">Delete</button></span>
                        <span><a asp-controller="Admin" asp-action="AllSemesters"><input type="button" class="btn btn-info" value="Cancel"></a></span>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
